import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 48,
    borderColor: "gray",
    borderWidth: 1,
    borderRadius: 8,
    backgroundColor: "#F5F7F9",
    padding: 16,
    fontSize: 16,
    fontWeight: "400",
  },
});
